(Definir (Facto n)
    "Retourne la factorielle de l’entier `n`, version récursive."
    "ProfGra"
    (Si (<= n 1)
     Alors (Retourner 1)
     Sinon (Retourner (* n (Facto (- n 1))))
    )
)
(Afficher "Vous voulez la factorielle de quel entier ?")
(Afficher (Facto (Nombre (Demander))))
